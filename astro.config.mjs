// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://lucasgonzalo.github.io',
  build: {
    format: 'directory'
  }
});
